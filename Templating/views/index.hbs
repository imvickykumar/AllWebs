<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>



{{#each data as |i|}}
    {{i.fruit}}
    {{#if i.price}}
        <div>
            {{i.price}}
        </div>
    {{else}}
        <div>

        </div>

    {{/if}}
{{/each}}

</body>
</html>
